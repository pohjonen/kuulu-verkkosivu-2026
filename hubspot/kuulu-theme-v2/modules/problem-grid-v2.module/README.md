# Problem Grid v2

Source candidate:

- `hubspot/source-theme/modules/cinematic_problem_list.module`

Current status:

- forked into `kuulu-theme-v2`
- field model normalized toward structured repeater cards
- media moved behind explicit toggles and media type selection
- styling aligned to token-based `theme-tokens.css` / `theme-components.css`

Next checks:

- validate field schema in HubSpot after upload
- compare rendered output against live homepage “Tunnistatko nämä” section
- decide whether icon handling should stay symbolic or migrate to a stricter icon choice model
